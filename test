SELECT LOWER(TRIM(COALESCE(
         element_at(custom_tags,'APP_NAME'),
         element_at(custom_tags,'MSDB_APP_NAME'),
         element_at(custom_tags,'APP_CODE'),
         element_at(custom_tags,'ENV_NAME'),
         'unknown'
       ))) AS team_value,
       COUNT(*) AS n
FROM system.billing.usage
GROUP BY team_value
ORDER BY n DESC
LIMIT 50;
